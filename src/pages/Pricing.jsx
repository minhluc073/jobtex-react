import React from 'react';
import PropTypes from 'prop-types';

Pricing.propTypes = {
    
};

function Pricing(props) {
    return (
        <div>
            
        </div>
    );
}

export default Pricing;